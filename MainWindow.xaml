<Window x:Class="ServerLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Server Launcher" Height="700" Width="1000"
        Background="{StaticResource PrimaryBrush}"
        Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterScreen">
    
    <Grid>
        <!-- Header -->
        <Grid Height="70" VerticalAlignment="Top" Background="{StaticResource SecondaryBrush}">
            <Border BorderBrush="#30363D" BorderThickness="0,0,0,2" VerticalAlignment="Center">
                <StackPanel Margin="30,15">
                    <TextBlock Text="SERVER LAUNCHER" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock x:Name="StatusText" Text="Ready" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Main Content -->
        <Grid Margin="0,70,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Servers Panel -->
            <Border Grid.Column="0" Background="{StaticResource SecondaryBrush}" Margin="15,15,7,15" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8">
                <StackPanel Margin="20">
                    <TextBlock Text="SERVERS" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,15"/>
                    
                    <!-- Server Buttons -->
                    <Border Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="15" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="API Server" FontSize="12" Foreground="{StaticResource TextBrush}" FontWeight="Bold" Margin="0,0,0,8"/>
                            <Button Content="▶ Start" Click="StartApiServer_Click" Style="{StaticResource LaunchButton}" Margin="0,0,0,5"/>
                            <Button Content="⏹ Stop" Click="StopApiServer_Click" Style="{StaticResource LaunchButton}" Background="{StaticResource ErrorBrush}"/>
                            <Ellipse x:Name="ApiServerStatus" Width="12" Height="12" Fill="{StaticResource ErrorBrush}" Margin="0,10,0,0" HorizontalAlignment="Left"/>
                            <TextBlock x:Name="ApiServerStatusText" Text="Offline" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="15" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Web Server" FontSize="12" Foreground="{StaticResource TextBrush}" FontWeight="Bold" Margin="0,0,0,8"/>
                            <Button Content="▶ Start" Click="StartWebServer_Click" Style="{StaticResource LaunchButton}" Margin="0,0,0,5"/>
                            <Button Content="⏹ Stop" Click="StopWebServer_Click" Style="{StaticResource LaunchButton}" Background="{StaticResource ErrorBrush}"/>
                            <Ellipse x:Name="WebServerStatus" Width="12" Height="12" Fill="{StaticResource ErrorBrush}" Margin="0,10,0,0" HorizontalAlignment="Left"/>
                            <TextBlock x:Name="WebServerStatusText" Text="Offline" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="15" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Database" FontSize="12" Foreground="{StaticResource TextBrush}" FontWeight="Bold" Margin="0,0,0,8"/>
                            <Button Content="▶ Start" Click="StartDatabase_Click" Style="{StaticResource LaunchButton}" Margin="0,0,0,5"/>
                            <Button Content="⏹ Stop" Click="StopDatabase_Click" Style="{StaticResource LaunchButton}" Background="{StaticResource ErrorBrush}"/>
                            <Ellipse x:Name="DatabaseStatus" Width="12" Height="12" Fill="{StaticResource ErrorBrush}" Margin="0,10,0,0" HorizontalAlignment="Left"/>
                            <TextBlock x:Name="DatabaseStatusText" Text="Offline" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Button Content="⚙ Settings" Click="OpenSettings_Click" Style="{StaticResource LaunchButton}" Width="100" HorizontalAlignment="Left" Margin="0,15,0,0"/>
                </StackPanel>
            </Border>

            <!-- Output Panel -->
            <Border Grid.Column="1" Background="{StaticResource SecondaryBrush}" Margin="7,15,15,15" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8">
                <StackPanel Margin="20">
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="OUTPUT" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        <Button Content="🗑 Clear" Click="ClearOutput_Click" Style="{StaticResource LaunchButton}" HorizontalAlignment="Right" Padding="10,5" FontSize="11"/>
                    </Grid>

                    <Border Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Height="550">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="OutputText" 
                                       Text="[Ready]&#10;" 
                                       Foreground="{StaticResource AccentBrush}" 
                                       FontFamily="Courier New" 
                                       FontSize="11" 
                                       Margin="10,10,10,10"
                                       TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
